# Python dependencies for AI_Part.
#
# ROS 2 dependencies are intentionally not listed here. Source/install them via
# ROS Jazzy packages instead:
#   source /opt/ros/jazzy/setup.bash
#   sudo apt install ros-jazzy-rclpy ros-jazzy-sensor-msgs ros-jazzy-cv-bridge
#
# CARLA's Python API is version-sensitive. Prefer the wheel/egg that ships with
# the CARLA version you run, or install it separately if you use
# collect_data_complete.py.

torch>=2.0
torchvision>=0.15
numpy>=1.24,<2.0
opencv-python>=4.6
matplotlib>=3.6
tqdm>=4.64
Pillow>=10.0
